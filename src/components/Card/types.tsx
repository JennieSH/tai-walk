import { ScenicSpotCard } from "@/types/scenic-spot";
import { RestaurantCard } from "@/types/restaurant";

interface CardProps {
  card: ScenicSpotCard | RestaurantCard;
}

export type { CardProps };
